{
  "name": "info-board",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "collect": "node scripts/collect.mjs",
    "test": "node --test test/*.test.js",
    "test:tap": "node --test --test-reporter=tap test/*.test.js"
  }
}
