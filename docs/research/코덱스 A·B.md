info-board — 검토 결과 중 A·B만 판단 필요. 나머지(C~F)는 문서에 있다.

git pull --ff-only
sed -n '/^## A\./,/^## C\./p' work-log/2026-08-28-검토-238f79b.md

■ 뿌리는 하나다

08-27 기록이 한 날짜 안에서 두 배치로 갈라져 있다.

  2026-08-27   16건  09:59 KST   (게임이 16개이던 때의 정규 실행)
  2026-08-27   59건  20:09 KST   (75개로 늘린 뒤 들어온 실행)
  2026-08-28   75건  10:39 KST

양쪽 날에 다 있는 75개 중 59개의 짝이 20:09 → 10:39, 즉 14.5시간이다.
대표값 CS2 는 09:59 → 10:39 라 40분이고, 그래서 C24 판정 자체는 안전하다.
깨지는 것은 그 옆 두 패널이다.

■ A. movers — 화면 6행이 전부 14.5시간 비교다

  src/view/panels.js:20 (movers) · :25 (compare)

  Awesomenauts              20:09 → 10:39  +42
  SMITE                     20:09 → 10:39  +803
  American Truck Simulator  20:09 → 10:39  +4,349
  Eternal Return            20:09 → 10:39  -10,820
  Human Fall Flat           20:09 → 10:39  -918
  Black Myth: Wukong        20:09 → 10:39  -17,469

  6행 중 6행. 화면은 이것을 "어제 대비" 라고 적는다. 규칙 3-1 이 ±214% 로
  실측해 둔 시각 편향이 하루치 변화라는 이름으로 나가고 있다.

■ B. timeBias — 라벨이 59/75 행과 다르다

  src/view/panels.js:345
    if (recordAt === null || r.fetchedAt < recordAt) recordAt = r.fetchedAt;

  recordAt 이 최솟값이라, 시각이 섞이면 가장 이른 것 하나가 전체 라벨이 된다.
  패널은 "정규 측정 09:59" 라고 쓰는데 75행 중 59행이 20:09 기록이다.
  화면에 뜨는 10행 중 4행이 불일치 (Awesomenauts, Farming Simulator 25·22,
  Euro Truck Simulator 2, Antichamber).

  그리고 더 나쁜 게 하나 있다. 20:09 기록의 짝인 표본은 16:30 이다.
  표본이 기록보다 3시간 39분 이르다. 패널은 "이 시각에 늘어난/줄어든" 이라고
  쓰는데 그 59행은 시간 순서가 반대다. 방향 서사가 뒤집혀 있다.

■ 문서와 사실이 어긋나 있다

  docs/DECISIONS.md:266 이 이걸 정확히 예측해 뒀다.

    "오늘(2026-08-27) 수집기를 다시 돌리면 안 된다 … 그 59개가 10:10 이
     아니라 지금 시각으로 오늘 칸에 들어간다 … 그 차이의 대부분이 하루치
     변화가 아니라 시각 차이가 된다."

  예측대로 일어났는데 문서는 아직 예측인 채다. 예측과 사실 중 하나는
  갱신돼야 한다. 기록 자체는 지우면 안 된다 — 지우는 게 조작이다.

■ 네가 정할 것

  1. 같은 날짜 안에서 시각이 갈린 기록을 비교에 쓸지, 임계를 둘지.
     둔다면 몇 시간까지를 같은 측정으로 볼지 — 측정 정책이라 네 판단이다.
     movers 는 이미 skipped 를 세어 화면에 적으므로 자리는 있다.
  2. 감추지 않고 드러내는 쪽(행마다 20:09 → 10:39 · 14.5h 표기)으로 갈지.
     규칙 7 이 "대조가 안 된다는 사실을 화면에 적는다" 로 간 것과 같은 태도다.
  3. timeBias 라벨을 어떻게 할지. 섞였다고 적을지, 표본과 같은 배치만 남길지.
     recordFetchedAt 이 이미 행마다 실려 있어 화면이 바로 쓸 수 있다.
  4. DECISIONS.md:266 을 예측에서 사실로 고쳐 적을지.

■ 재현

node -e "
const j=require('./data/records.json');
const kst=i=>new Intl.DateTimeFormat('ko-KR',{timeZone:'Asia/Seoul',hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date(i));
const g={}; for(const r of j.records.filter(x=>x.date==='2026-08-27')) (g[kst(r.fetchedAt)]??=[]).push(r.appid);
for(const [k,v] of Object.entries(g)) console.log(k, v.length+'건');
"

코드는 안 건드렸다. C~F 도 같은 문서에 파일:줄 · 재현 · 영향 · 제안으로 있다.

■ 덧 — 고치지 않는 것도 선택지다

A·B 는 238f79b 의 결함이 아니다. 그 경계는 오히려 앞으로 이런 일을 막는 쪽이다
— 이미 저장된 게임을 다시 부르지 않으니까. A·B 는 경계가 생기기 전에 들어온
08-27 데이터가 화면에서 드러난 것이다.

그래서 선택지가 하나 더 있다. **화면에 사실만 적고 코드는 두는 것.**
08-29 정규 실행이 들어오면 08-28 ↔ 08-29 비교는 양쪽 다 단일 배치라 문제가
저절로 사라진다. 08-27 의 얼룩만 남는다.

다만 제출 심사가 오늘·내일이면 심사자가 보는 것이 그 얼룩이다. 그 시점 판단은
네 몫이다.
