# 작업 기록 — 여기서 시작한다

**이 파일은 목차다.** 내용을 여기 옮겨 적지 않는다 — 같은 내용을 두 곳에 두면
언젠가 갈라지고, 그게 T02 에서 겪은 사고다. 각 항목은 **실제로 그 내용이 사는 곳**을
가리킨다.

마지막 갱신 2026-08-28 08:00 KST

커밋 해시와 테스트 개수는 여기 적지 않는다. 그것도 상태이고, 적어 두면 낡는다.

---

## 지금 딱 하나만 한다면

**2026-08-28 10:10 KST 이후에 공개 주소를 열고 `날짜별 기록` 이 두 줄인지 본다.**

**https://myeongjundev.github.io/info-board/**

두 줄이면 카드 4·5 가 닫히고 과제가 사실상 끝난다.
한 줄이면 [오후 기록](2026-08-27-오후-확장과-시각편향.md) 의 `내일 아침에 할 일` 절.

**`collect.mjs` 를 손으로 돌리지 않는다.** 동시접속자는 순간값이라 아무 때나 재면
"매일 같은 시각에 잰다" 는 약속이 깨진다. 10:10 KST 에 Actions 가 넣는다.

---

## 현재 상태

**여기에 적지 않는다.** [docs/DECISIONS.md](../docs/DECISIONS.md) 에 있다.

전에는 이 자리에 커밋·테스트 수·카드별 통과 여부를 적어 두었는데, 2026-08-28 에
확인해 보니 테스트 162건(실제 201건)·커밋 `7c92f6a`(실제 `f80f5ab`)·능동 작업
19.0h(실제 20.5h)로 **세 값이 전부 낡아 있었다.**

상태를 두 곳에 적으면 갈라진다. T02 에서 난이도 실험 하나가 세 문서에 미시작·
미통과·통과로 각각 적혀 있었던 것이 그것이고, 이 파일이 같은 길로 가고 있었다.
그래서 숫자를 고치는 대신 **자리를 없앤다.**

이 폴더의 날짜별 기록은 **그날의 판단을 남기는 것**이고 현재 상태가 아니다.
지나간 기록은 고쳐 쓰지 않는다 — 고쳐 쓰면 무엇을 왜 바꿨는지가 사라진다.

---

## 어디에 무엇이 적혀 있나

| 찾는 것 | 문서 |
|---|---|
| **지금 상태 · 모든 결정과 근거** | [docs/DECISIONS.md](../docs/DECISIONS.md) ← **상태를 적는 유일한 곳** |
| 에이전트가 지킬 규칙 7가지 | [CLAUDE.md](../CLAUDE.md) |
| 찾아서 고친 결함 13개와 그 패턴 | [docs/DEFECTS.md](../docs/DEFECTS.md) |
| 심사자용 30초 검증 안내 | [docs/VERIFY.md](../docs/VERIFY.md) |
| 원자료·저장값·계산값·화면값 대조 | [docs/CROSSCHECK.md](../docs/CROSSCHECK.md) |
| 소재를 고르며 남긴 조사 | [docs/research/](../docs/research/) |

`DECISIONS.md` 안에서 자주 찾게 되는 절들이다.

- 소재를 Steam 으로 바꾸며 **잃은 것**
- 설계 불변식 (`Reading`, `STATE`, `FAULT`)
- **비율의 분모는 아는 것이어야 한다**
- 참고 화면(MSN 날씨)에서 가져온 것과 **바꾼 것**
- 장르 — 넣기로 했고 대신 지킨 셋
- **하루 한 번은 "인기" 가 아니라 "그 순간" 이다** — 실측
- **DB 를 쓰지 않는다** — 파일 크기 한계 표
- 순위 이동 — 분모가 바뀐 것을 하락으로 만들지 않는다
- 개선 명세에서 **안 가져온 것**

---

## 날짜별 작업 기록

| 문서 | 무엇 |
|---|---|
| [2026-08-27 · 소재 확정과 수집기](2026-08-27-소재-확정과-수집기.md) | Stack Overflow 시절. **대체됨** — 지우지 않고 표시만 달았다 |
| [2026-08-27 · Steam 전환과 첫 화면](2026-08-27-steam-전환과-첫-화면.md) | 오전. 소재 전환, 하네스, 첫 화면, 결함 13개 |
| [2026-08-27 · 오후 확장과 시각 편향](2026-08-27-오후-확장과-시각편향.md) | 오후. 확장 패널, 시각 편향 실측 |
| [2026-08-28 · 성인 분류 접기와 경로 결론](2026-08-28-성인분류-접기와-경로-결론.md) | **학원에서 이어갈 때 여기부터 읽는다** |

---

## 코드 구조

```
src/source/     출처. definition.js 하나만 고치면 다른 데이터로 옮겨진다      486줄
src/state/      기록. 날짜+appid 키, 중복 방지, 손상 격리, 비교               162줄
src/view/       화면 계산. React 를 import 하지 않아 브라우저 없이 테스트된다   595줄
src/ui/         React 컴포넌트. 계산을 하지 않는다                    20파일 1607줄
scripts/        collect.mjs (Actions) · probe-hours.mjs (손으로)
test/           node --test, 의존성 없음                          7파일 1379줄
data/           records.json (진실의 원천) · timeprobe.json (별개 표본)
```

**`view` 와 `ui` 를 나눈 이유**는 변화량·방향·경과시간 계산을 브라우저 없이
검증하기 위해서다. 테스트할 수 없는 자리에 계산이 있으면 손계산 대조를 못 한다.
`review` 스킬이 `ui` 안에 나눗셈이 생기면 잡는다.

---

## 화면에 있는 것

스크롤 깊이가 곧 신뢰 깊이가 되게 배치했다. 탭을 쓰지 않는다 — 브리프가
`한 화면에 보인다` 를 카드 1·5 의 통과 기준으로 못박았기 때문이다.

```
상단 띠     로고 · 게임 검색 · 설정(대표 게임 고르기)
구획 나브   현재 · 잰 날 · 대조 · 장애 · 시각 · 장르 · 순위 · 움직임 · 순위 이동 · 오래된 게임
            ↑ 누르면 갈아끼우지 않고 그 구획으로 내려간다

1층  대표값 · 단위 · 잰 날 · 잰 시각 · 어제와 비교        15초 판정 구역
     출처 · 날짜별 기록
2층  잰 날 카드 줄 · 대조(접이식) · 장애 재현 5종          근거
3층  같은 날 다른 시각 · 장르 · 순위 · 움직임 ·            확장
     순위 이동 · 아직 살아 있는가
```

**API 를 더 붙이지 않았다.** 3층까지 전부 같은 기록 파일에서 나온다.
예외는 표지 그림 하나이고 그건 값이 아니다 ([CLAUDE.md](../CLAUDE.md) 5번 예외).

---

## 명령어

```bash
npm test                    # 단위 테스트. 의존성 없음
npm run dev                 # 화면 (5174)
node scripts/collect.mjs    # 하루 한 건. 손으로 돌리지 않는다 ⚠
node scripts/probe-hours.mjs   # 시각별 표본. 아무 때나 돌려도 된다
```

`/review` 스킬은 커밋 전에 돌린다. 오늘 실제로 다섯 번 돌렸고 **그중 두 번이
결함을 잡았다** — `ui` 안의 나눗셈, 그리고 리뷰 자신의 사각지대.

---

## 남은 것

1. **10:10 KST — 둘째 기록.** 이것만 되면 카드 4·5 가 닫힌다
2. **AI 3줄** — 본인 몫. 재료는 [오후 기록](2026-08-27-오후-확장과-시각편향.md) 의
   `AI 3줄 재료` 절에 표로 있다
3. 그 뒤 30분짜리 하나 — 줄세우기 각 줄에 값 변화율

**확인 못 한 것 7가지**도 오후 기록에 있다. 그중 둘(스크롤스파이, lazy 그림)은
집에서 화면을 눈으로 보면 바로 지워진다.
