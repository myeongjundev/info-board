# GAME PULSE — 작업 규칙

SKT ALEPH T04 · 오늘의 진짜 정보판. 이 파일은 **에이전트가 매번 읽는 규칙**이다.
과제 원문은 [docs/research/오늘의 진짜 정보판.md](docs/research/오늘의%20진짜%20정보판.md),
현재 상태는 [docs/DECISIONS.md](docs/DECISIONS.md) 에 있다.

## 무엇을 만드는가

Steam 동시접속자를 **매일 같은 시각에 한 번 재서**, 그 값을 믿어도 되는 근거와 함께
보여주는 정보판. 대표값은 Counter-Strike 2 (appid 730) 다.

## 절대 깨면 안 되는 것

### 1. Reading 불변식

화면에 보이는 값·단위·출처·날짜·조회시각은 **언제나 하나의 객체에서** 나온다.
따로 전달하지 않는다.

```js
Reading = { value, unit, date, appid, sourceUrl, sourceLabel, timezone, fetchedAt }
```

`date` 는 잰 날, `fetchedAt` 은 잰 시각이다. 묶어서만 다루면 "값은 새 것인데 시각은
옛 것" 이 구조적으로 불가능해진다. `Reading` 이 없으면 숫자 자리에 넣을 것이 없다.

### 2. 실패를 값으로 바꾸지 않는다

- 호출이 실패하면 **기록하지 않는다.** 0 을 넣지 않는다.
- 값이 없는 것과 값이 0 인 것은 다르다. 아무도 안 하는 게임은 진짜로 0 명이다.
- 정상값이 한 번도 없으면 숫자 자리는 `—` 로 둔다.

### 3. 순간값은 과거를 잴 수 없다

동시접속자는 부르는 순간의 값이다. 어제 10:10 에 몇 명이었는지는 **그때 재지
않았으면 영영 모른다.** 지금 재서 어제 칸에 넣는 것은 측정이 아니라 조작이다.
`assertMeasurableNow()` 가 호출 전에 막는다.

### 3-1. 하루 한 번은 "인기" 가 아니라 "그 순간" 이다

10:10 KST = **01:10 UTC** 다. 한국·일본 아침, 유럽 새벽, 미국 동부 전날 저녁이다.
그래서 우리 숫자는 `이 게임을 몇 명이 하는가` 가 아니라
**`그 순간 접속해 있던 사람이 몇 명인가`** 다.

실측(2026-08-27, 01:00 → 07:30 UTC)으로 **방향이 게임마다 반대**임을 확인했다.

```
PUBG          102,136 → 320,864   +214%    (아시아 위주)
Apex Legends   65,473 → 111,647    +71%
HELLDIVERS 2   62,103 →  35,036    -44%    (서구 위주)
Left 4 Dead 2  25,947 →  17,045    -34%
```

그러므로 **순위표를 "인기 순위" 라고 쓰지 않는다.** 화면의 `같은 날, 다른 시각`
패널이 이 사실을 숫자로 보여준다. 표본은 `scripts/probe-hours.mjs` 로 만들고
`data/timeprobe.json` 에 따로 쌓인다.

**측정 시각은 바꾸지 않는다.** 매일 같은 시각에 재야 어제와 오늘의 차이에 시각
차이가 안 섞인다. 중립적인 시각은 없다 — 어느 시각을 골라도 누군가는 자고 있다.

### 4. `records.json` 을 손으로 채우지 않는다

과거 날짜를 몰아 넣으면 "서로 다른 실제 날짜의 기록" 을 형식만 만족하고 실질은
어긴다. 기다린 기간은 능동 작업시간에 포함되지 않으므로 손해도 아니다.

### 5. 문서화되지 않은 엔드포인트를 쓰지 않는다

동시접속자 데이터에 쓰는 것은 하나뿐이다. 가격 실험은 5-1의 임시 예외다.

```
GET https://api.steampowered.com/ISteamUserStats/GetNumberOfCurrentPlayers/v1/?appid=<id>
```

공식 문서가 있고 키가 없다. 게임 이름을 얻자고 `store.steampowered.com/api/*` 같은
비공식 경로를 부르지 않는다. 이름은 `GAMES` 표에 우리가 적어 둔 것이고, 원자료와
이어 주는 열쇠는 appid 다.

나무위키 `sidebar.json` 과 KOBIS `.do` 를 이 기준으로 떨어뜨렸다.

#### 예외 하나 — 값이 아닌 자산

표지 그림만 예외다. `src/source/artwork.js` 의 Steam CDN 경로는 공식 문서에 없다.

이 규칙이 막으려는 것은 **값이 검증 불가능한 경로에서 오는 것**이고, 그림은 값이
아니다. 그래서 아래 셋을 지키는 동안만 예외다. 하나라도 깨지면 예외가 아니다.

1. **화면의 어떤 숫자도 그림에 기대지 않는다.** 값·단위·날짜·잰 시각·비교·순위는
   전부 `records.json` 에서 온다. CDN 이 통째로 죽어도 그중 하나도 안 바뀐다.
2. **게임 이름은 글자로 따로 남는다.** 그림이 이름을 대신하지 않는다.
3. **안 오면 조용히 사라진다.** 깨진 네모도 대체 상자도 남기지 않는다 —
   그림이 안 온 것이 자료가 안 온 것처럼 보이면 안 된다.

이 경로에서 그림 말고 다른 것을 가져오지 않는다. 테스트가 `.jpg` 만 나가는지 본다.

#### 5-0. 비공식 경로에 대한 결론 — 2026-08-28 확정

5-1 ~ 5-4 가 "제출 전에 다시 판단한다" 로 미뤄 두었던 것을 여기서 닫는다. **유지한다.**

근거는 "우리 프로젝트에 공식 출처도 있으니까" 가 아니다. 그건 심사에서 못 쓰는
답이다. 구역마다 따로 서야 한다.

**진짜 근거 셋.**

1. **과제가 경로를 제한하지 않는다.** 카드 2 원문이 `특정 API나 호출 경로는
   지정하지 않는다` 이다. 비공식 경로 금지는 과제가 아니라 **우리가 만든 규칙**이고,
   그것은 대표값의 출처를 고를 때 세운 기준이다.
2. **채점되는 값은 전부 공식 경로 위에 있다.** 카드 1 이 문서와 사용 조건을 요구하는
   대상은 대표값 하나다. 그 값은 `GetNumberOfCurrentPlayers` 에서 오고, 기록·비교·순위가
   전부 거기서 나온다. 보조 구역은 채점되는 숫자를 하나도 만들지 않는다.
3. **각 구역이 화면에서 스스로 밝힌다.** `문서화되지 않은 공개 프로모션 응답` 처럼
   적혀 있다. 공식이라고 부르지 않는 한 이 구분은 유지된다.

**그래서 "나무위키는 왜 안 되고 이건 되나" 의 답은 이것이다.** 나무위키는 대표값
자리에 놓으려 했고, 이 경로들은 보조 구역에 있으며 채점되는 값을 만들지 않는다.

**비밀값 0건 검사 — 2026-08-28 실측.** 스트리밍이 키를 쓰면서 이 항목이 자명한
참이 아니게 되어 실제로 훑었다.

```
작업트리    변수 이름과 ${{ secrets.X }} 참조만. 값 0건
빌드 산출물  긴 문자열 둘은 React 내부 이름. 값 0건
data/*.json  secret·token·client_id 0건
git 이력     커밋 33개 전체 blob 스캔 0건
```

**이 결론이 깨지는 조건.** 보조 구역의 값이 대표값 계산에 들어가거나, 화면이 비공식
경로를 공식이라고 부르기 시작하면 다시 판단한다.

### 5-1. 임시 실험 예외 — 한국 할인 페이지

**2026-08-27 사용자가 명시적으로 허용했다. 제출 전에 반드시 유지 여부를 다시
판단한다.** `scripts/collect-discounts.mjs`만 아래 비공식 Store 경로를 쓸 수 있다.

```
GET https://store.steampowered.com/api/appdetails?appids=<id>&cc=KR&l=koreana
```

- 목적은 `GAMES` 고정 목록과 수집 당시 Steam 공식 동시접속 상위 100개 중 한국에서
  현재 할인 중인 게임을 할인 페이지에 서로 구분해 모으는 것이다.
- 고정 목록을 Steam 전체 할인이라고 부르지 않고, Top 100은 수집 당시 순위임을 적는다.
- 가격·할인율·통화·국가·appid·출처·조회시각은 하나의 PriceReading으로 묶는다.
- 실패·가격 없음·무료·정가는 서로 다르다. 실패를 0원이나 0%로 만들지 않는다.
- 원화 가격은 응답의 1/100원 정수이므로 `initialMinor`·`finalMinor`라고 저장한다.
- `data/discounts.json`은 현재 스냅샷이고 동시접속자 `records.json`과 섞지 않는다.
- 성공률이 80% 미만이면 기존 파일을 덮지 않는다.
- 다른 비공식 Store 경로로 넓히지 않는다.

**제출 전 체크:** 비공식 경로 허용 여부를 과제 평가 기준과 다시 대조한다. 허용하기
어렵다면 할인 페이지·수집기·워크플로 단계·이 절을 함께 제거한다. 공식 위젯만 남기는
방안도 있다.

### 5-2. 임시 실험 예외 — Epic 기간 한정 무료 배포

**2026-08-28 사용자가 무료 배포 구역 추가를 요청했다. 제출 전에 반드시 유지 여부를
다시 판단한다.** 공식 무료 게임 페이지는 자동 요청에 Cloudflare 보안 화면을 반환하므로,
`scripts/collect-epic-free.mjs`만 아래 공개 프로모션 응답을 쓸 수 있다.

```
GET https://store-site-backend-static.ak.epicgames.com/freeGamesPromotions?locale=ko&country=KR&allowCountries=KR
```

- 한국 Epic Games Store에서 **현재 진행 중인 기간 한정 100% 무료 배포**만 대상으로 한다.
- 상시 무료 플레이 게임과 아직 시작하지 않은 다음 배포는 섞지 않는다.
- 제목·원래 가격·통화·시작·종료·이미지·상점 링크·조회시각을 한 Reading으로 묶는다.
- 수집 장애나 응답 형식 변경을 무료 게임 0개로 만들지 않고 기존 파일을 지킨다.
- `data/epic-free.json`은 Steam 할인·동시접속자와 섞지 않는 현재 스냅샷이다.
- 이 경로를 Epic의 문서화된 공식 API라고 부르지 않는다.

**제출 전 체크:** 비공식 응답 허용 여부를 평가 기준과 다시 대조한다. 허용하기 어렵다면
Epic 무료 배포 UI·수집기·워크플로 단계·이 절을 함께 제거하고 공식 무료 게임 페이지
링크만 남긴다.

### 5-3. 임시 실험 예외 — Steam 무료 소장·무료 플레이 주말

**2026-08-28 사용자가 기간 한정 무료 소장과 무료 플레이 주말 확인을 요청했다. 제출 전에 유지 여부를
다시 판단한다.** `scripts/collect-steam-free.mjs`만 Steam 상점 검색 결과 응답을 쓴다.

- `specials=1 · maxprice=free · hidef2p=1 · category1=998` 조건으로 상시 무료와
  게임 외 항목을 제외한다.
- 검색 결과에서 정가가 있고 100% 할인된 게임만 무료 소장 후보로 저장한다.
- 100% 할인이 아닌 항목은 곧바로 무료 주말로 간주하지 않는다. 상세 상점 페이지에
  `Free Weekend`, `무료 주말`, `무료 플레이` 등 임시 플레이 근거가 있을 때만 별도 목록에 저장한다.
- 무료 주말의 구매 할인 종료와 무료 플레이 종료는 다를 수 있다. 무료 주말 전용 종료 키를
  확인하지 못하면 종료 시각을 비워 두고 Steam 상점에서 확인하도록 안내한다.
- 무료 주말의 소유권·기간 해석은 Steamworks 공식 문서
  `https://partner.steamgames.com/doc/marketing/discounts/freeweekends`를 기준으로 한다.
- 검색 결과가 정상적으로 0건이면 `현재 없음`이고, 응답 형식 오류는 기존 파일을 지킨다.
- 무료 소장의 종료 epoch도 상점 페이지에서 확인하지 못하면 임의 시각을 만들지 않는다.
- 이 검색 응답을 문서화된 Steam Web API라고 부르지 않는다.

### 5-4. Steam 이 공개한 판매 차트 — 자료는 공식, 경로는 아님

**절 제목을 고쳤다.** 전에는 `Steam 공식 판매 차트 페이지` 였는데, 이 둘은 다른
주장이라 섞으면 안 된다.

- **자료**: Steam 이 스스로 공개한 공식 차트가 맞다
- **경로**: 문서화된 API 가 아니라 페이지의 서버 렌더링 응답을 파싱한다

규칙 5 가 `비공식 경로를 공식 API 라고 부르지 않는다` 고 못박은 자리가 여기다.
화면에서도 `Steam이 공개한 공식 차트를 페이지 응답에서 읽는다 (문서화된 API 아님)`
처럼 둘을 갈라 적는다.

`scripts/collect-sales-charts.mjs`는 Steam이 공개하는 차트 페이지 세 곳의 서버 렌더링
데이터를 읽는다. 별도의 판매량이나 매출액을 추정하지 않는다.

```
https://store.steampowered.com/charts/topselling/KR
https://store.steampowered.com/charts/topselling/global
https://store.steampowered.com/charts/
```

- 현재 차트는 한국·글로벌 각각 매출 Top 20만 저장한다.
- 주간 차트는 한국 공식 Top 20과 전주 순위·신규 진입·연속 순위권 주차를 저장한다.
- 월간은 Steam이 공개한 해당 월 인기 신작 목록이다. 전체 게임의 월간 판매 순위나
  항목별 세부 순위를 만들지 않는다.
- 차트는 판매 수량 순위가 아니라 매출 순위다. Steam이 공개하지 않은 판매량·매출액은
  화면과 파일 어디에도 채우지 않는다.
- 페이지 구조가 바뀌거나 필수 Top 20이 빠지면 기존 스냅샷을 덮지 않는다.
- 화면에는 조회 시각과 원자료 링크를 함께 둔다.

#### 출시 캘린더 검색 예외

같은 수집기의 `이번 달 신작 / 다음 달 출시 예정` 구역은 공식 매출 차트가 아니라 Steam
상점의 문서화되지 않은 검색 결과 응답을 사용한다. 사용자가 허용한 Steam Store 응답
실험 범위이며 제출 전에 유지 여부를 다시 검토한다.

- `popularnew`와 `popularcomingsoon` 인기 검색 표본을 각각 최대 100개 읽고, KST 달력상
  이번 달·다음 달에 해당하는 게임만 최대 20개 표시한다. Steam 전체 출시작 목록이 아니다.
- 게임명·이미지·한국 가격·할인율·공개 출시일·상점 링크만 저장한다.
- 개발사가 일까지 공개하면 일 단위, 월까지만 공개하면 월 단위로 보존한다. `출시 예정`처럼
  달력 범위를 확인할 수 없는 문구에는 임의 날짜를 붙이지 않는다.
- 다음 달 항목은 공개 출시일순으로 정렬한다. 가격이 없으면 `가격 미정`으로 표시한다.
- 검색 응답 형식이 바뀌면 기존 스냅샷을 덮지 않는다.
- 출시일의 공개 정밀도는 Steamworks 공식 문서
  `https://partner.steamgames.com/doc/store/release_dates`를 기준으로 해석한다.

### 5-5. 공식 스트리밍 게임 Top 10

`scripts/collect-streaming.mjs`는 공식 인증 정보가 연결된 플랫폼만 수집한다. CHZZK와
Twitch 각각 상위 라이브 방송 100개 표본을 게임 카테고리별로 묶고, 표본 시청자 합계가 큰
게임 10개만 화면에 표시한다. 이는 플랫폼 전체의 확정 시청률이 아니라 수집 시각의
상위 방송 표본이다.

- CHZZK: `CHZZK_CLIENT_ID`·`CHZZK_CLIENT_SECRET`으로 공식 `/open/v1/lives`를
  최대 100개까지 순회한다.
- Twitch: `TWITCH_CLIENT_ID`·`TWITCH_CLIENT_SECRET`으로 App Access Token을 받은 뒤
  공식 `helix/streams?first=100`을 읽는다.
- 게임 카테고리가 아닌 방송, 카테고리 없는 방송은 집계하지 않는다.
- 인증 부재·수집 실패를 시청자 0명이나 빈 정상 순위로 바꾸지 않는다.
- Client Secret과 API 키는 저장소 파일에 쓰지 않고 GitHub Actions Secrets로만 넣는다.
- `data/streaming-history.json`은 플랫폼별 최근 168회 정상 표본만 보존한다. 현재 순위와
  바로 전 정상 표본 사이에서만 순위·시청자 증감을 계산하고, 첫 표본에는 변화를 만들지 않는다.
- 두 플랫폼 공통 게임은 동일 제목 또는 검토된 한글·영문 별칭으로만 연결한다. 플랫폼
  시청자 수를 더하거나 하나의 통합 순위를 만들지 않는다.

### 5-6. 성인 분류 항목은 접어서 보여준다 — 순위는 그대로 둔다

공개 순위에는 우리가 고르지 않은 것이 들어온다. 2026-08-28 한국 매출 20위에 성인
게임이 올라와 제목과 표지가 공개 주소에 그대로 떠 있었다.

**판별.** `content descriptor` 에 **3 또는 4** 가 있으면 접는다. 실측 근거는 이렇다.

```
4924510  [1,3,4,5]  → Steam 이 login/?redir=agecheck 로 막는다
3718190  [1,3,4,5]  → Steam 이 막는다
3308670  [1,5]      → Steam 이 막지 않는다
730      [2,5]      → Steam 이 막지 않는다
```

3·4 만 쓰면 Steam 의 자체 게이트와 정확히 일치한다. **1 이나 2 로 넓히지 않는다** —
정상 게임까지 접히면 우리가 자료를 가리는 쪽이 된다. `test/contentDescriptors.test.js`
가 이 표본을 그대로 들고 있다.

**무엇을 접고 무엇을 안 접는가.**

- 접는 것: 제목 글자, 표지 그림
- **안 접는 것: 순위·가격·할인율·상점 링크.** 숫자는 하나도 안 움직인다

이 규칙이 다루는 것은 표시이지 값이 아니다. 규칙 5 의 표지 그림 예외가 "그림은 값이
아니다" 라고 쓴 것과 같은 자리다. 공개된 순위를 지우면 그게 조작이다.

**왜 접는 쪽이 원본에 가까운가.** Steam 은 이 항목의 상점 페이지를 비로그인
방문자에게 안 보여준다. 우리 화면은 나이 확인 없이 제목과 표지를 띄우고 있었다 —
**원본보다 더 많이 보여주고 있었다.** 접는 것은 Steam 의 표시 방식에 맞추는 것이다.

**펼치기 버튼을 두지 않는다.** 우리가 나이 확인을 흉내 낼 방법이 없다. 상점 링크는
그대로 두므로 보려는 사람은 Steam 으로 가고 확인은 Steam 이 한다.

**접었다는 사실을 화면이 말한다.** 차트 아래 안내에 규칙과 이 화면에서 접힌 개수를
적는다. 조용히 가리면 그것도 숨기는 것이다.

**descriptor 를 한 건도 못 읽으면 스냅샷을 덮지 않는다.** 응답 형식이 바뀐 것으로 본다.
그대로 두면 접혀야 할 항목이 펼쳐진 채 배포된다. `assertDescriptorsPresent` 가 막는다.

### 6. `LIVE` 라고 쓰지 않는다

Steam 은 CORS 를 열어 주지 않아 브라우저가 직접 못 부른다. Actions 가 하루 한 번
받아 커밋하므로 방문자가 보는 값은 **최대 24시간 지난 값**이다. 거기 `LIVE` 를
붙이는 것이 이 과제가 잡아내려는 바로 그 거짓말이다.

`2026-08-27 10:10 KST 측정` 처럼 잰 시각을 적는다.

### 7. 순간값이라 원자료 대조가 안 된다는 사실을 화면에 적는다

심사자가 출처 링크를 누르면 화면의 기록값과 다른 숫자가 나온다. 40분 사이에도
572,811 → 551,673 으로 움직였다. 숨기면 카드 1 체크리스트에 걸리고, 드러내면
과제가 원하는 태도가 된다.

## 상태를 적는 곳

**`docs/DECISIONS.md` 하나뿐이다.** 다른 문서는 여기를 링크만 한다.

T02 에서 난이도 실험 하나가 세 문서에 각각 미시작·미통과·통과로 적혀 있었고,
T03 은 루트에 프롬프트·리뷰·체크리스트 md 가 일곱 개 흩어져 362칸이 전부 미체크로
남았다. 같은 일을 반복하지 않는다.

- 체크리스트를 만들면 **그 자리에서 채운다.** 못 채울 것은 만들지 않는다.
- "확인하지 못한 범위" 를 정직하게 남긴다.

## 구조

```
src/source/     출처. definition.js 하나만 고치면 다른 데이터로 옮겨진다
src/state/      기록. 날짜+appid 키, 중복 방지, 손상 격리, 비교
src/view/       화면에 넣기 직전의 순수 계산. React 를 import 하지 않는다
src/ui/         React 컴포넌트. 계산을 하지 않는다
scripts/        Actions 가 돌리는 진입점
test/           node --test
data/           진실의 원천
```

`src/view` 와 `src/ui` 를 나누는 이유는 화면 계산을 브라우저 없이 테스트하기
위해서다. 변화량·방향·경과시간 같은 것은 전부 `view` 에서 계산하고 `ui` 는 받아
그린다.

## 확인 방법

```bash
npm test                  # node --test. 의존성 없음
node scripts/collect.mjs  # 값이 제대로 오는지
```

## 커밋

- 값이 안 바뀌는 날은 커밋이 없는 것이 정상이다.
- `process.exit()` 을 쓰지 않는다. 열린 소켓을 끊으면서 libuv 가 죽어 종료 코드가
  엉키고, 그러면 정상인 날에 CI 가 빨간불이 된다. `process.exitCode` 만 세운다.

### 5-7. 통화를 우리가 고르지 않는다

차트 가격은 `formatted_final_price` 를 **그대로 옮겨 그대로 보여준다.**
`finalMinor / 100` 처럼 화면에서 나누고 통화 기호를 붙이지 않는다.

이유는 실측이다. 차트 URL 의 `cc=KR` 은 **통화를 바꾸지 않는다** — 한국에서
`cc=KR`·`cc=US`·`cc=JP` 로 불러 같은 `₩ 63,840` 을 받았다. 통화를 정하는 것은
**호출한 IP** 이고, 그래서 로컬(한국) 수집은 KRW, Actions(미국 러너) 수집은 USD 다.
응답에 통화 코드 필드는 없다.

전에 이것을 모르고 USD 센트를 `/100` 해서 원화 기호를 붙였고, `₩79,800` 짜리
게임이 **`₩70` 으로 공개 주소에 나갔다.**

- `currency` 는 확신할 때만 채운다. `₩` → `KRW`, 그 밖은 `null`.
  `$` 를 USD 로 단정하지 않는다 — 모르는 것에 이름을 붙이면 같은 결함이 된다
- `initialMinor`·`finalMinor` 는 보존하되 **단위는 `currency` 다.** 항상 원이 아니다
- 이 화면의 가격을 **`한국 가격` 이라고 부르지 않는다.** 순위는 한국 차트 주소에서
  오므로 통화와 무관하고, 그 구분을 화면에 적는다
- 가격이 붙은 항목이 있는데 표기를 한 건도 못 읽으면 `assertPriceTextPresent` 가
  기존 스냅샷을 지킨다

`appdetails` 를 쓰는 5-1·5-2 는 다르다. 그쪽은 `cc=KR` 이 실제로 먹고 응답에
`"currency":"KRW"` 가 있다. **경로마다 따로 확인한다.**
