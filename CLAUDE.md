# GAME PULSE — 작업 규칙

SKT ALEPH T04 · 오늘의 진짜 정보판. 이 파일은 **에이전트가 매번 읽는 규칙**이다.
과제 원문은 [docs/research/오늘의 진짜 정보판.md](docs/research/오늘의%20진짜%20정보판.md),
현재 상태는 [docs/DECISIONS.md](docs/DECISIONS.md) 에 있다.

## 무엇을 만드는가

Steam 동시접속자를 **매일 같은 시각에 한 번 재서**, 그 값을 믿어도 되는 근거와 함께
보여주는 정보판. 대표값은 Counter-Strike 2 (appid 730) 다.

## 절대 깨면 안 되는 것

### 1. Reading 불변식

화면에 보이는 값·단위·출처·날짜·조회시각은 **언제나 하나의 객체에서** 나온다.
따로 전달하지 않는다.

```js
Reading = { value, unit, date, appid, sourceUrl, sourceLabel, timezone, fetchedAt }
```

`date` 는 잰 날, `fetchedAt` 은 잰 시각이다. 묶어서만 다루면 "값은 새 것인데 시각은
옛 것" 이 구조적으로 불가능해진다. `Reading` 이 없으면 숫자 자리에 넣을 것이 없다.

### 2. 실패를 값으로 바꾸지 않는다

- 호출이 실패하면 **기록하지 않는다.** 0 을 넣지 않는다.
- 값이 없는 것과 값이 0 인 것은 다르다. 아무도 안 하는 게임은 진짜로 0 명이다.
- 정상값이 한 번도 없으면 숫자 자리는 `—` 로 둔다.

### 3. 순간값은 과거를 잴 수 없다

동시접속자는 부르는 순간의 값이다. 어제 10:10 에 몇 명이었는지는 **그때 재지
않았으면 영영 모른다.** 지금 재서 어제 칸에 넣는 것은 측정이 아니라 조작이다.
`assertMeasurableNow()` 가 호출 전에 막는다.

### 4. `records.json` 을 손으로 채우지 않는다

과거 날짜를 몰아 넣으면 "서로 다른 실제 날짜의 기록" 을 형식만 만족하고 실질은
어긴다. 기다린 기간은 능동 작업시간에 포함되지 않으므로 손해도 아니다.

### 5. 문서화되지 않은 엔드포인트를 쓰지 않는다

쓰는 것은 하나뿐이다.

```
GET https://api.steampowered.com/ISteamUserStats/GetNumberOfCurrentPlayers/v1/?appid=<id>
```

공식 문서가 있고 키가 없다. 게임 이름을 얻자고 `store.steampowered.com/api/*` 같은
비공식 경로를 부르지 않는다. 이름은 `GAMES` 표에 우리가 적어 둔 것이고, 원자료와
이어 주는 열쇠는 appid 다.

나무위키 `sidebar.json` 과 KOBIS `.do` 를 이 기준으로 떨어뜨렸다.

### 6. `LIVE` 라고 쓰지 않는다

Steam 은 CORS 를 열어 주지 않아 브라우저가 직접 못 부른다. Actions 가 하루 한 번
받아 커밋하므로 방문자가 보는 값은 **최대 24시간 지난 값**이다. 거기 `LIVE` 를
붙이는 것이 이 과제가 잡아내려는 바로 그 거짓말이다.

`2026-08-27 10:10 KST 측정` 처럼 잰 시각을 적는다.

### 7. 순간값이라 원자료 대조가 안 된다는 사실을 화면에 적는다

심사자가 출처 링크를 누르면 화면의 기록값과 다른 숫자가 나온다. 40분 사이에도
572,811 → 551,673 으로 움직였다. 숨기면 카드 1 체크리스트에 걸리고, 드러내면
과제가 원하는 태도가 된다.

## 상태를 적는 곳

**`docs/DECISIONS.md` 하나뿐이다.** 다른 문서는 여기를 링크만 한다.

T02 에서 난이도 실험 하나가 세 문서에 각각 미시작·미통과·통과로 적혀 있었고,
T03 은 루트에 프롬프트·리뷰·체크리스트 md 가 일곱 개 흩어져 362칸이 전부 미체크로
남았다. 같은 일을 반복하지 않는다.

- 체크리스트를 만들면 **그 자리에서 채운다.** 못 채울 것은 만들지 않는다.
- "확인하지 못한 범위" 를 정직하게 남긴다.

## 구조

```
src/source/     출처. definition.js 하나만 고치면 다른 데이터로 옮겨진다
src/state/      기록. 날짜+appid 키, 중복 방지, 손상 격리, 비교
src/view/       화면에 넣기 직전의 순수 계산. React 를 import 하지 않는다
src/ui/         React 컴포넌트. 계산을 하지 않는다
scripts/        Actions 가 돌리는 진입점
test/           node --test
data/           진실의 원천
```

`src/view` 와 `src/ui` 를 나누는 이유는 화면 계산을 브라우저 없이 테스트하기
위해서다. 변화량·방향·경과시간 같은 것은 전부 `view` 에서 계산하고 `ui` 는 받아
그린다.

## 확인 방법

```bash
npm test                  # node --test. 의존성 없음
node scripts/collect.mjs  # 값이 제대로 오는지
```

## 커밋

- 값이 안 바뀌는 날은 커밋이 없는 것이 정상이다.
- `process.exit()` 을 쓰지 않는다. 열린 소켓을 끊으면서 libuv 가 죽어 종료 코드가
  엉키고, 그러면 정상인 날에 CI 가 빨간불이 된다. `process.exitCode` 만 세운다.
